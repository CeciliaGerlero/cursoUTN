const letras = ['a', 'b', 'c'];
const letras2 = ['n', 'm', 't'];

const letrastotales = letras.concat(letras2);
console.log(letras)
console.log(letras2)
console.log(letrastotales)

// se usa para unir lso array que tenemos. Devuelve un nuevo array